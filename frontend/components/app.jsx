import React from 'react';
import NavBar from './nav_bar';

const App = () => (
    <div>
        <header>
            <NavBar/>
        </header>

        <section>
        </section>
    </div>
);

export default App;